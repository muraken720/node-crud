class ThrottleBucket {
  function initialize() {
    this.lastRequestTime = 0;
    this.clear();
  }

  function increment(time) {
    this.lastRequestTime = time;
    this.count++;
  }

  function clear() {
    this.count = 0;
  }
}
